#include <stdio.h>
/**
 * main - print alphabet in lowercase
 *
 * Return: an integer value of 0
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar("\n")
	return (0);
}
